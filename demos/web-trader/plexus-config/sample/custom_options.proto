
syntax = "proto2";

package sample;

import "interop/descriptor.proto";

/**
* Extend core metadata with custom 'alias' options to support shorter references
*/
extend .interop.ApplicationOptions {
	optional string app_alias = 1500;
}

extend .interop.ProvidedMethodOptions {
	optional string provided_method_alias = 1500;
}